# 🧩 Components

Gjenbrukbare komponenter, hooks, og utility functions for prosjektet.

## Struktur

- `hooks/` - Custom React hooks
- `ui/` - UI-komponenter
- `utils/` - Utility functions
- `types/` - TypeScript types og interfaces

## Eksempel på innhold

### Hooks
- `useDebounce.ts` - Debounce hook for søk/input
- `useLocalStorage.ts` - LocalStorage management
- `useIntersectionObserver.ts` - Scroll animations

### UI Components
- `Button.tsx` - Custom button component
- `Modal.tsx` - Reusable modal
- `LoadingSpinner.tsx` - Loading states

### Utils
- `formatPrice.ts` - Price formatting
- `dateHelpers.ts` - Date utilities
- `validation.ts` - Form validation

## Bruk

Legg komponenter her som kan gjenbrukes på tvers av prosjektet. 
AI kan referere til disse når du spør om implementering.

